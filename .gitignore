# 원본 Excel 데이터 (대용량, git 제외)
data/*.xlsx
data/*.csv

# DuckDB는 git에 포함 (Streamlit Cloud 배포용)
# data/fisis.duckdb → 포함

__pycache__/
*.pyc
*.pyo
.env
venv/
.streamlit/secrets.toml

# 불필요 파일
screenshot*.png
PLAN.md

# 평가 파이프라인 결과물 (API 응답 데이터, 대용량 차트 포함)
eval/results/

# FISIS 레지스트리 스냅샷 — 라이브 API 호출 결과. 기본적으로 git 제외.
# 검증용으로 커밋하고 싶으면 `git add -f scripts/fisis_registry_snapshot.json`
scripts/fisis_registry_snapshot.json
